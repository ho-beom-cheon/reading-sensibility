# 버전별 게이트 체크리스트

## v0.1 - 제품 정의/PRD 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 제품 문제, 가치, MVP 포함/제외 범위, 성공 기준을 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.2 - 사용자 플로우/IA 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 사용자가 홈에서 카드 저장까지 이동하는 전체 경로와 정보구조를 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.3 - 화면/와이어프레임 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 각 화면의 UI 컴포넌트, 입력값, 검증, 에러 메시지를 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.4 - 데이터 모델/상태 모델 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: Book, ReadingMoment, GeneratedCard, Template, OCR 세션과 상태 전이를 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.5 - API/오류 계약 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 클라이언트-서버 연동 API, 요청/응답/에러 코드를 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.6 - OCR/AI/추천 명세 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: OCR 처리 정책, AI 입력/출력, 템플릿 추천 점수식을 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.7 - 렌더링/템플릿 시스템 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 카드 렌더링 파이프라인, 템플릿 카탈로그, 해상도/오버플로우 정책을 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.8 - 보안/개인정보/저작권 안전장치 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 저장 정책, 원본 이미지 삭제, private visibility, 고지 문구를 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v0.9 - QA/베타/운영 지표 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 테스트 시나리오, 지표, 로그, 베타 운영 기준을 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.

## v1.0 - 릴리스/핸드오프 고정

### 진입 조건

- [ ] 이전 버전 산출물이 존재한다.
- [ ] 이전 버전 체크리스트가 완료되었다.
- [ ] 현재 버전에서 해결할 미정 항목이 명확하다.

### 완료 조건

- [ ] 목표: 출시 가능 조건, 개발 백로그, 검수 체크리스트, 운영 인수인계를 확정한다.
- [ ] 산출물이 `docs/` 또는 `specs/`에 저장되었다.
- [ ] 개발 이슈로 분해 가능한 수준이다.
- [ ] 이전 버전과 충돌하지 않는다.
- [ ] 다음 버전으로 넘길 open item이 정리되었다.
